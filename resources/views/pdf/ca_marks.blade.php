<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>CA Marks Report</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: #fff;
            color: #000;
        }

        .wrapper {
            width: 100%;
            max-width: 750px;
            margin: 0 auto;
        }

        .container {
            padding: 10px;
        }

         h2 {
            text-align: center;
            text-transform: uppercase;
            letter-spacing: 1px;
            font-size: 16px;
            margin-bottom: 10px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            font-size: 13px;
            margin-bottom: 10px;
        }

        th, td {
            border: 1px solid #999;
            padding: 8px;
            text-align: left;
        }

        th.theading {
            background-color: #e2e8f0;
            text-transform: uppercase;
            font-size: 12px;
            text-align: center;
        }

        tr:nth-child(even) td {
            background-color: #f9f9f9;
        }

        .footer {
            position: fixed;
            bottom: 15mm;
            left: 0;
            right: 0;
            text-align: center;
            font-style: italic;
            font-size: 12px;
            color: #555;
        }
    </style>
</head>
<body>
    <div class="wrapper">
        <img src="{{ public_path('/storage/'.getSetting('LETTER_HEAD')) }}" alt="LetterHead" style="width: 100%; height: 160px;">
        <div class="container">
            <h2>CA Marks Report - {{ $course->name }}</h2>

            <table>
                <thead>
                    <tr>
                        <th class="theading">#</th>
                        <th class="theading">Student Name</th>
                        <th class="theading">Matricule</th>
                        <th class="theading">Level</th>
                        <th class="theading">Mark / {{ getSetting('TOTAL_CA_MARK') }}</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach($caMarks as $index => $mark)
                        <tr>
                            <td>{{ $index + 1 }}</td>
                            <td>{{ $mark->student->user->name ?? 'N/A' }}</td>
                            <td>{{ $mark->student->matricule ?? 'N/A' }}</td>
                            <td>{{ $mark->student->level->name ?? 'N/A' }}</td>
                            <td>{{ $mark->mark }}</td>
                        </tr>
                    @endforeach
                </tbody>
            </table>

        </div>
    </div>

    <div class="footer">
        Generated by {{ config('app.name') }} on {{ now()->setTimezone($timezone)->format('Y-m-d') }} at {{ now()->setTimezone($timezone)->format('H:i a') }}
    </div>
</body>
</html>
